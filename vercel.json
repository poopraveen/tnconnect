{
  "buildCommand": "prisma generate && next build",
  "framework": "nextjs",
  "build": {
    "env": {
      "DATABASE_URL": "postgresql://placeholder:placeholder@placeholder.example.com/placeholder"
    }
  }
}
